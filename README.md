# NEKTO.ME MONITOR

![Banner](assets/banner.svg)

<div align="center">

**Система мониторинга и управления чатами Nekto.me в реальном времени**

[![Python](https://img.shields.io/badge/python-3.12+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-00a393.svg)](https://fastapi.tiangolo.com/)

[English](#english) | [Русский](#russian)

</div>

---

## 🇷🇺 Russian

### 📋 Описание

**Nekto.me Monitor** — это продвинутая система для мониторинга и управления множественными клиентами Nekto.me. Проект позволяет перехватывать, пересылать сообщения между ботами и управлять диалогами через интуитивный веб-интерфейс в реальном времени. Поддерживаются любые комбинации полов: **MF, FM, MM, FF**.

### 🔄 Принцип работы

Этот проект работает по принципу атаки **MITM** (Man-in-the-Middle - "человек посередине"):

```
┌─────────────────┐    ┌──────────┐    ┌─────┐    ┌──────────┐    ┌─────────────────┐
│ Собеседник 1    │◄──►│ Leader   │◄──►│ ВЫ  │◄──►│ Follower │◄──►│ Собеседник 2    │
│ (реальный user) │    │ (1й бот) │    │     │    │ (2й бот) │    │ (реальный user) │
└─────────────────┘    └──────────┘    └─────┘    └──────────┘    └─────────────────┘
```

**Как это работает:**

1. Вы создаете **любую пару** клиентов (MF, FM, MM, FF)
2. **Leader** (1-й клиент в конфиге) начинает поиск первым
3. Когда Leader находит собеседника, **Follower** (2-й клиент) начинает поиск
4. Сообщения автоматически пересылаются между диалогами
5. Вы можете наблюдать оба разговора через веб-интерфейс
6. При необходимости можете взять ручное управление любым клиентом

> **Поддерживаются все комбинации:** M↔F, F↔M, M↔M, F↔F

### ✨ Основные возможности

- 🤖 **Множественные клиенты**: Одновременное управление несколькими ботами (мужские/женские профили)
- 💬 **Перехват сообщений**: Автоматическая ретрансляция сообщений между клиентами
- � **Аудио MITM**: Перехват аудио-диалогов с записью в MP3 и live-прослушиванием
- �🎮 **Веб-панель управления**: Современный dashboard для мониторинга и контроля
- 🔄 **Режимы работы**: Автоматический и ручной режим управления диалогами
- 📊 **Логирование**: Автоматическое сохранение всех диалогов в JSON формате
- 🔍 **Полнотекстовый поиск**: Быстрый поиск по логам через Whoosh
- ⚡ **Real-time обновления**: WebSocket соединение для мгновенного обновления статуса
- 🎯 **Гибкая настройка**: Фильтры по возрасту, полу, интересам через config.ini
- 🔌 **FastAPI + SocketIO**: Современный асинхронный backend
- 🌐 **Поддержка прокси**: Индивидуальные прокси для каждого клиента
- ⏳ **Wait-for зависимости**: Клиент ждёт другого перед поиском

### 🖼️ Интерфейс

#### Dashboard (Управление ботами)
Главная панель управления мульти-клиентами. Показывает статус всех ботов, активные диалоги и позволяет брать управление в свои руки.
![Dashboard Preview](assets/dashboard.png)

#### Chat Logs (История диалогов)
Удобный просмотрщик сохраненных логов. Поддерживает поиск по тексту, фильтрацию по дате и полу.
![Chat Logs Preview](assets/chat_logs.png)

### 🚀 Быстрый старт

#### Требования

- Python 3.12 или выше ([скачать для Windows](https://www.python.org/downloads/))
- pip (менеджер пакетов Python)

**Установка Python на Linux:**

```bash
# Arch Linux
yay -S python312

# Ubuntu/Debian
sudo apt install python3.12
```

#### Установка

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/S1ii/nekto.me-multiMITM-web.git
cd nekto.me-multiMITM-web
```

2. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

3. **Получите токены Nekto.me:**

> **Важно:** Вам нужно получить **2 токена** для пары клиентов (любой комбинации полов)

Для каждого токена:

а) Откройте браузер в **режиме инкогнито**

б) Перейдите на [nekto.me/chat](https://nekto.me/chat)

в) Откройте **консоль разработчика** (F12)

г) Выполните команду:

```javascript
JSON.parse(localStorage.getItem("storage_v2"))["user"]["authToken"];
```

д) **Скопируйте токен** и **User-Agent**

User-Agent можно получить командой:

```javascript
navigator.userAgent;
```

е) **Важно:** Пообщайтесь 5-10 раз на этом аккаунте, чтобы система Nekto.me не заблокировала его как бота

ж) Повторите процесс в **другом браузере** или **другом профиле** для второго токена

4. **Настройте конфигурацию:**

Переименуйте `config.ini.example` в `config.ini`:

```bash
# Windows
ren config.ini.example config.ini

# Linux/Mac
mv config.ini.example config.ini
```

Отредактируйте `config.ini` и добавьте ваши токены и User-Agent:

```ini
[settings]
clients = client-1-M client-2-F
debug = False

[client/client-1-M]
token = ВАШ_ТОКЕН_1_ЗДЕСЬ
ua = ВАШ_USER_AGENT_1_ЗДЕСЬ
age = 18,25
wish-age = 18,25
sex = M
wish-sex = F
role = False
adult = True

[client/client-2-F]
token = ВАШ_ТОКЕН_2_ЗДЕСЬ
ua = ВАШ_USER_AGENT_2_ЗДЕСЬ
age = 18,25
wish-age = 18,25
sex = F
wish-sex = M
role = False
adult = True
```

> **Подсказка:** Порядок клиентов определяет роли: 1-й клиент = **Leader** (ищет первым), 2-й = **Follower**. Поддерживаются любые комбинации полов (MM, FF, MF, FM).

5. **Запустите приложение:**

```bash
# Запуск текстового чата
python web_server.py --chat

# Запуск аудио MITM
python web_server.py --audio

# Запуск текстового + аудио
python web_server.py --chat --audio

# С режимом отладки
python web_server.py --chat --audio --debug
```

6. **Откройте веб-панель:**

Перейдите по адресу: `http://localhost:8000`

### 🔧 Аргументы командной строки

| Аргумент    | Описание                                      |
|-------------|-----------------------------------------------|
| `--chat`    | Включить текстовый чат MITM                   |
| `--audio`   | Включить аудио чат MITM                       |
| `--debug`   | Включить подробное логирование                 |

> **Примечание:** Если не указать `--chat` или `--audio`, сервер запустится без MITM функционала.

### 🔐 Решение капчи

Nekto.me требует прохождение reCAPTCHA v2 при получении токена. Есть несколько способов:

#### Вариант 1: NoCaptchaAI (рекомендуется)

[NoCaptchaAI](https://nocaptchaai.com/) предоставляет **50 бесплатных капч** при регистрации (достаточно для создания 3-4 клиентов):

1. Зарегистрируйтесь на [nocaptchaai.com](https://nocaptchaai.com/)
2. Установите [browser extension](https://nocaptchaai.com/software)
3. Получите API ключ в личном кабинете
4. Используйте расширение при получении токенов

#### Вариант 2: Buster (бесплатно)

[Buster](https://github.com/dessant/buster) - расширение для браузера с решением капч через audio:

1. Установите расширение из [Chrome Web Store](https://chrome.google.com/webstore/detail/buster-captcha-solver-for/mpbjkejclgfgadiemmefgebjfooflfhl) или [Firefox Add-ons](https://addons.mozilla.org/firefox/addon/buster-captcha-solver/)
2. Настройте в опциях расширения
3. При появлении reCAPTCHA используйте Buster для автоматического решения

#### Вариант 3: Ручное решение

Просто решайте капчу вручную при получении каждого токена.

### ⚙️ Конфигурация

Пример настройки клиента в `config.ini`:

```ini
[settings]
clients = client-1-M client-2-F
auto_search = true  # Автоматический поиск при запуске (true/false)
debug = false

[client/client-1-M]
token = YOUR_NEKTO_TOKEN_HERE
ua = YOUR_USER_AGENT_HERE
age = 18,25
wish-age = 18,25
sex = M
wish-sex = F
role = False
adult = True

[client/client-2-F]
token = YOUR_NEKTO_TOKEN_HERE
ua = YOUR_USER_AGENT_HERE
age = 18,25
wish-age = 18,25
sex = F
wish-sex = M
role = False
adult = True
```

### 🎧 Audio MITM (nekto.me/audiochat)

Этот проект также умеет перехватывать **аудио-диалоги** из `nekto.me/audiochat` и сохранять их в MP3.

1. Получите **2 аудио токена** на странице https://nekto.me/audiochat

В DevTools → Console:

```javascript
console.log(
  JSON.parse(localStorage.getItem("storage_audio_v2")).user.authToken,
);
```

2. Заполните секцию `[audio]` в `config.ini` (пример есть в `config.ini.example`).

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. Запустите веб‑сервер (аудио MITM стартует автоматически, если настроен):

```bash
python web_server.py
```

5. Откройте веб‑панель: http://localhost:8000 → вкладка **Audio Logs** (раздел **Live Audio**).

> Аудио сохраняется в папку `audio_logs` в формате MP3.

_Опционально:_ можно запускать только аудио‑MITM через `python audio_server.py`.

**Параметры конфигурации:**

- `auto_search` - автоматически начинать поиск при запуске (true/false)
- `debug` - режим отладки с подробными логами (true/false)
- `age` - ваш возраст (диапазон)
- `wish-age` - желаемый возраст собеседника (диапазон)
- `sex` - ваш пол (M/F)
- `wish-sex` - желаемый пол собеседника (M/F)
- `role` - ролевые игры (true/false)
- `adult` - контент 18+ (true/false)

### 📁 Структура проекта

```
nekto.me-multiMITM-web/
├── src/                      # Исходный код
│   ├── __init__.py           # Package initialization
│   ├── client.py             # Класс клиента Nekto.me (текст)
│   ├── chat_manager.py       # Менеджер множественных диалогов
│   ├── config.py             # Загрузка конфигурации
│   ├── config_manager.py     # Управление конфигом в runtime
│   ├── search_index.py       # Whoosh полнотекстовый поиск логов
│   ├── utils.py              # Утилиты
│   └── audio/                # Модуль аудио MITM
│       ├── __init__.py       # Package exports
│       ├── audio_client.py   # WebSocket клиент nekto.me/audiochat
│       ├── audio_manager.py  # Менеджер аудио сессий
│       ├── config.py         # Конфигурация аудио
│       ├── dispatcher.py     # Диспетчер событий
│       ├── handlers.py       # Обработчики событий
│       ├── rtc.py            # WebRTC peer connection
│       ├── types.py          # Типы данных
│       └── utils.py          # Аудио утилиты
├── static/                   # Веб-интерфейс (модульная структура)
│   ├── index.html            # Dashboard HTML
│   ├── css/                  # Стили
│   │   ├── main.css          # Главный CSS файл
│   │   ├── base/             # Переменные, сброс, типографика
│   │   ├── components/       # Кнопки, бейджи, вкладки, инпуты
│   │   ├── layout/           # Шапка, контейнер
│   │   └── sections/         # Комнаты, логи, аудио
│   └── js/                   # JavaScript
│       ├── main.js           # Главный JS файл
│       └── modules/          # Модули
│           ├── rooms.js      # Логика комнат
│           ├── logs.js       # Логика логов чата
│           ├── audio.js      # Логика аудио чата
│           ├── audio-logs.js # Логика аудио логов
│           └── utils.js      # Общие утилиты
├── assets/                   # Визуальные ресурсы
│   └── banner.svg            # SVG баннер
├── web_server.py             # FastAPI веб-сервер (точка входа)
├── audio_server.py           # Отдельный запуск аудио MITM
├── config.ini.example        # Пример конфигурации
├── requirements.txt          # Зависимости
├── LICENSE                   # Лицензия
├── .gitignore                # Git ignore
Все диалоги автоматически сохраняются в директорию `chat_logs/` в формате JSON:

```json
{
  "client_token": "d95a1368cb",
  "client_sex": "M",
  "start_time": "2026-01-15T02:32:26",
  "end_time": "2026-01-15T02:45:13",
  "messages": [
    {
      "timestamp": "2026-01-15T02:32:30",
      "sender": "interlocutor",
      "message": "Привет!"
    }
  ]
}
```

### ⚠️ Важные замечания

- 🔒 **Безопасность**: Никогда не публикуйте `config.ini` с реальными токенами
- 📊 **Производительность**: Рекомендуется не более 10 одновременных клиентов
- 🌐 **Сеть**: Требуется стабильное интернет-соединение
- ⚖️ **Ответственность**: Используйте в соответствии с правилами Nekto.me

### 🐛 Устранение неполадок

**Проблема**: Клиент не подключается

- Проверьте валидность токена
- Убедитесь в наличии интернет-соединения
- Проверьте правильность User-Agent

**Проблема**: Сообщения не пересылаются

- Убедитесь, что оба клиента (Leader и Follower) подключены
- Проверьте логи в консоли (`debug = True` в config.ini)

### � Благодарности

Этот проект основан на оригинальной работе [pashtetx/nekto.me-spion](https://github.com/pashtetx/nekto.me-spion).

**Основные доработки:**

- ✨ Веб-интерфейс с real-time мониторингом через WebSocket
- 🎮 Ручное управление клиентами через dashboard
- 📊 Улучшенная система логирования диалогов
- 🔄 Менеджер множественных параллельных диалогов
- 🎨 Современный dark theme UI

### �📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

---

## 🇬🇧 English

### 📋 Description

**Nekto.me Monitor** is an advanced system for monitoring and managing multiple Nekto.me clients. The project enables message interception, relay between bots, and dialog management through an intuitive real-time web interface.

### ✨ Key Features

- 🤖 **Multiple Clients**: Simultaneous management of several bots (male/female profiles)
- 💬 **Message Interception**: Automatic message relay between clients
- 🎮 **Web Control Panel**: Modern dashboard for monitoring and control
- 🔄 **Operation Modes**: Automatic and manual dialog management modes
- 📊 **Logging**: Automatic saving of all dialogs in JSON format
- ⚡ **Real-time Updates**: WebSocket connection for instant status updates
- 🎯 **Flexible Configuration**: Filters by age, gender, interests via config.ini
- 🔌 **FastAPI + SocketIO**: Modern asynchronous backend

### 🚀 Quick Start

#### Requirements

- Python 3.12 or higher
- pip (Python package manager)

#### Installation

1. **Clone the repository:**

```bash
git clone https://github.com/S1ii/nekto.me-multiMITM-web.git
cd nekto.me-multiMITM-web
```

2. **Install dependencies:**

```bash
pip install -r requirements.txt
```

3. **Configure:**

```bash
cp config.ini.example config.ini
```

Edit `config.ini` and add your tokens and client settings.

4. **Run the application:**

```bash
python web_server.py
```

5. **Open web panel:**

Navigate to: `http://localhost:8000`

### � Credits

This project is based on the original work by [pashtetx/nekto.me-spion](https://github.com/pashtetx/nekto.me-spion).

**Major enhancements:**

- ✨ Web interface with real-time WebSocket monitoring
- 🎮 Manual client control via dashboard
- 📊 Enhanced dialog logging system
- 🔄 Multiple parallel dialog manager
- 🎨 Modern dark theme UI
- 📁 Professional project structure

### �📄 License

This project is licensed under the MIT License. See the [LICENSE](LICENSE) file for details.

---

<div align="center">

**Made with ❤️ for chat automation**

⭐ Star this repo if you find it useful!

</div>
